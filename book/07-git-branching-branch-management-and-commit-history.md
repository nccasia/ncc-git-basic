# Git branch management and commit history

## 1. Git branch management

```
$ git branch

$ git branch --all

$ git branch -d
```

## 2. Commit history

```
$ git log

$ git log --oneline

$ git log --oneline --graph --all
```
